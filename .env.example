# =============================================================================
# Draftboard Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Prisma Postgres)
# -----------------------------------------------------------------------------
# Connection string for your Prisma Postgres database.
# For local development, run `npx prisma dev` to get a local connection URL.
# See: https://pris.ly/d/connection-strings
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"

# -----------------------------------------------------------------------------
# NextAuth.js
# -----------------------------------------------------------------------------
# Secret used to encrypt JWTs and session cookies.
# Generate one with: openssl rand -base64 32
NEXTAUTH_SECRET="generate-a-random-secret-here"

# The canonical URL of your app. Used for OAuth callbacks and redirects.
NEXTAUTH_URL="http://localhost:3000"

# -----------------------------------------------------------------------------
# Authentication Provider
# -----------------------------------------------------------------------------
# Draftboard supports exactly ONE auth provider per deployment.
# Configure one of the following options:
#
# OPTION 1: Credentials (default)
#   No additional variables needed. Users sign up with email/password
#   and are invited via invite links.
#
# OPTION 2: Okta SSO
#   Set all three AUTH_OKTA_* variables. Users sign in with their Okta
#   account and are auto-provisioned on first login.
#
# OPTION 3: Google Workspace
#   Set AUTH_GOOGLE_CLIENT_ID and AUTH_GOOGLE_CLIENT_SECRET.
#   Optionally set AUTH_GOOGLE_ALLOWED_DOMAIN to restrict sign-in
#   to a specific Google Workspace domain.
#
# IMPORTANT: Do not configure both Okta and Google at the same time.
# The app will refuse to start if both are detected.

# --- Okta SSO ---
# AUTH_OKTA_CLIENT_ID=your_okta_client_id
# AUTH_OKTA_CLIENT_SECRET=your_okta_client_secret
# AUTH_OKTA_ISSUER=https://your-org.okta.com

# --- Google Workspace ---
# AUTH_GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
# AUTH_GOOGLE_CLIENT_SECRET=your_google_client_secret
# AUTH_GOOGLE_ALLOWED_DOMAIN=yourcompany.com

# -----------------------------------------------------------------------------
# Cloudflare R2 Storage (for file uploads)
# -----------------------------------------------------------------------------
# These are required for image/file upload functionality.
# Get these from your Cloudflare dashboard → R2 → API Tokens.

# Your Cloudflare account ID (found in the dashboard URL or account overview)
R2_ACCOUNT_ID=your_cloudflare_account_id

# R2 API token credentials (create an API token with R2 read/write access)
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key

# The name of your R2 bucket
R2_BUCKET_NAME=your_bucket_name

# Public URL for accessing uploaded files.
# Typically: https://<account_id>.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://your_account_id.r2.cloudflarestorage.com
